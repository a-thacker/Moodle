# For local dev only (`npm run dev`): copy to hub/.env (gitignored).
# Deploys get these from Netlify environment variables instead.
# ANON key only — never the service-role key.
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=
