# Dev/test-only dependencies (not installed in the production image).
#   pip install -r requirements.txt -r requirements-dev.txt
#   python -m tests.test_grocery
-r requirements.txt
aiosqlite>=0.20,<1.0   # in-memory async DB for tests (prod uses Postgres)
httpx>=0.27,<1.0       # required by fastapi.testclient
